# This file ensures the data/postgres directory exists when cloning the repository.
# The actual PostgreSQL data files are ignored by .gitignore to prevent data loss.
# Run `docker-compose up -d postgres` and `npm run db:seed` to initialize the database.
