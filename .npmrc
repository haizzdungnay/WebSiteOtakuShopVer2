# NPM Configuration for better development experience
# Reduces install time and improves caching

# Use offline cache if available
prefer-offline=true

# Increase max workers
maxsockets=50

# Don't audit packages on install
audit=false

# Use lockfile when installing
use-lock-file=true
